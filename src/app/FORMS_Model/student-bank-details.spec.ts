import { StudentBankDetails } from './student-bank-details';

describe('StudentBankDetails', () => {
  it('should create an instance', () => {
    expect(new StudentBankDetails()).toBeTruthy();
  });
});
