import { ParentBankDetails } from './parent-bank-details';

describe('ParentBankDetails', () => {
  it('should create an instance', () => {
    expect(new ParentBankDetails()).toBeTruthy();
  });
});
